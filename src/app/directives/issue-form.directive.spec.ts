import { IssueFormDirective } from './issue-form.directive';

describe('IssueFormDirective', () => {
  it('should create an instance', () => {
    const directive = new IssueFormDirective();
    expect(directive).toBeTruthy();
  });
});
